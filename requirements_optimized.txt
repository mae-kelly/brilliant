# Core WebSocket and Async Libraries (working versions)
websockets>=11.0.0
aiohttp>=3.8.0
uvloop>=0.17.0
aiodns>=3.0.0
aioredis>=2.0.1

# Essential data processing (stable versions)
numpy>=1.24.0,<2.2.0
scipy>=1.10.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Blockchain essentials
web3>=6.11.0
eth-abi>=4.2.1
eth-utils>=2.2.0
hexbytes>=0.3.0
multicall>=0.7.0

# Performance monitoring
prometheus-client>=0.17.0
psutil>=5.9.0

# Replace problematic packages with alternatives
charset-normalizer>=3.0.0  # Instead of cchardet
talib-binary>=0.4.19       # Pre-compiled TA-Lib instead of ta-lib
numba>=0.57.0              # Keep if needed for performance

# Optional ML (comment out if TensorFlow causes issues)
# tensorflow>=2.13.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
ujson>=5.8.0
orjson>=3.9.0
